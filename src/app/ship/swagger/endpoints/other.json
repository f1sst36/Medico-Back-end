{
    "/storage/files/:fileName": {
        "get": {
            "tags": ["Other"],
            "description": "localhost:8080/storage/files/slide3.png"
        },
        "parameters": [
            {
                "name": "fileName",
                "in": "query",
                "description": "File name",
                "required": true,
                "schema": {
                    "type": "string"
                }
            }
        ]
    }
}
